IDENTIFICATION DIVISION.
PROGRAM-ID. QUICK-SORT.

DATA DIVISION.
WORKING-STORAGE SECTION.
01  ARR-SIZE     PIC 9(2) VALUE 5.
01  ARR          PIC 9(3) OCCURS 5 TIMES.

PROCEDURE DIVISION.
    MOVE 034 TO ARR(1).
    MOVE 012 TO ARR(2).
    MOVE 045 TO ARR(3).
    MOVE 023 TO ARR(4).
    MOVE 009 TO ARR(5).

    PERFORM QUICK-SORT-PROC USING 1 ARR-SIZE.

    DISPLAY "ARRAY ORDENADO:".
    PERFORM VARYING I FROM 1 BY 1 UNTIL I > ARR-SIZE
        DISPLAY ARR(I)
    END-PERFORM.

    STOP RUN.

QUICK-SORT-PROC.
    USING LOW HIGH.
    *> Implementação do Quick Sort aqui
    EXIT.
